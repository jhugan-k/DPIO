#include <bits/stdc++.h>
using namespace std;
class solution{
    public:
    int maxUniqueSubarray(vector<int> &nums){
        unordered-set<int>seen;
        int left = 0; int currSum = 0; int maxSum = 0;
        for(int right = 0; right < nums.size(); right++0{

        })
    }
}